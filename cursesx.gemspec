Gem::Specification.new do |s|
  s.name = %q{cursesx}
  s.version = "003"

  s.required_rubygems_version = Gem::Requirement.new(">= 0") if s.respond_to? :required_rubygems_version=
  s.authors = ["Keita Yamaguchi"]
  s.date = %q{2008-07-27}
  s.email = %q{keita.yamaguchi@gmail.com}
  s.files = [".gemified", "History.txt", "License.txt", "README.txt", "Rakefile", "lib/cursesx.rb"]
  s.has_rdoc = true
  s.homepage = %q{http://rubyforge.org/projects/cursesx/}
  s.require_paths = ["lib"]
  s.rubyforge_project = %q{cursesx}
  s.rubygems_version = %q{1.2.0}
  s.summary = %q{cursesx is an useful extension of standard curses.rb.}

  if s.respond_to? :specification_version then
    current_version = Gem::Specification::CURRENT_SPECIFICATION_VERSION
    s.specification_version = 2

    if current_version >= 3 then
    else
    end
  else
  end
end
